---
name: aiko-org
description: Alias of /aiko-origin. Switch to origin mode so Aiko (original) starts by default. Use when the user types "/aiko-org".
---

# /aiko-org（`/aiko-origin` の別名）

このスキルは `/aiko-origin` のエイリアスです。動作は `/aiko-origin` と完全に同じです。
このコマンドが呼ばれたら、`.claude/skills/aiko-origin/SKILL.md` を読み、その手順を実行してください。
